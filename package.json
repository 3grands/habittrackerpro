{
+  "type": "commonjs",
   "scripts": {
+    "dev":     "vite --port 5000 --host 0.0.0.0",
+    "build":   "vite build && cross-env NODE_ENV=production esbuild server/start.ts --bundle --platform=node --format=cjs --external:node:* --external:lightningcss --external:*.node --outfile=dist/server.js",
+    "start":   "node dist/server.js",
+    "preview": "vite preview --port=$PORT",
+    "check":   "tsc",
+    "db:push": "drizzle-kit push"
   },
   {
     "type": "commonjs",
     "scripts": {
       "dev":     "vite --port 5000 --host 0.0.0.0",
       "build":   "vite build && npx esbuild server/start.ts --bundle --platform=node --format=cjs --external:node:* --external:lightningcss --external:*.node --outfile=dist/server.js",
       "start":   "node dist/server.js",
       "preview": "vite preview --port=$PORT",
       "check":   "tsc",
       "db:push": "drizzle-kit push"
     },
     "dependencies": {
       /* your existing deps… */
     },
     "devDependencies": {
       /* your existing devDeps… */
     },
     "optionalDependencies": {
       /* …etc */
     }
   }


